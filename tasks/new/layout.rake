namespace :new do
  desc "Generate new layout."
  task :layout do
    # Create layouts/[layout].erb
    # Create content/styles/[_layout].scss
    # Update content/styles/styles.scss
    
    # Have the output be like nanoc with "Create" and "Update" in color
    # and the whole column indented - just like nanoc!
  end
end